# مولّد الخط الحركي — Kinetic Typography Generator

أداة ويب تعرض نصوصاً بتأثيرات بصرية متحركة ومتنوعة. مبنية باستخدام [p5.js](https://p5js.org/).

---

## 🚀 طريقة التشغيل

تحتاج فقط إلى Python (مثبّتة مسبقاً على معظم الأجهزة):

```bash
python -m http.server 8000
```

ثم افتح المتصفح على: **http://localhost:8000**

> **ملاحظة:** لا يمكن فتح `index.html` مباشرة بدون سيرفر — المتصفح يمنع تحميل الخطوط المحلية بدونه.

---

## ✏️ كيف تخصّص المشروع

**كل الإعدادات في ملف واحد فقط:** `js/config.js`

```javascript
// ── النص المعروض ──────────────────────────────────
// استخدم \n للسطر الجديد
var starterText = "نصك\nهنا";

// ── الألوان (كود Hex) ──────────────────────────────
var backgroundColorHex = "#000000"; // لون الخلفية
var foregroundColorHex = "#ffffff"; // لون النص

// ── سرعة الحركة ───────────────────────────────────
// 30 افتراضي — كلما زاد الرقم كلما زادت السرعة
var animationSpeed = 30;

// ── اختيار الخط ───────────────────────────────────
// اكتب رقماً من 0 إلى 6
// 0: TT Bluescreens   1: TT Travels   2: Inter
// 3: Agrandir         4: ApocLC       5: BaseNeueTrial   6: Cairo
var fontIndex = 0;
```

بعد أي تعديل، **أعد تحميل الصفحة** في المتصفح.

---

## 📁 هيكل المشروع

```
├── index.html              → الصفحة الرئيسية (لا تحتاج تعديلها)
├── style.css               → تنسيق بسيط للصفحة
│
├── js/
│   ├── config.js           → ⚙️  إعداداتك — عدّل هنا فقط
│   │
│   ├── sketch_flash.js     → المحرك الرئيسي (p5.js setup/draw)
│   ├── animators.js        → دوال الحركة الرياضية (easing)
│   ├── textures.js         → رسم النص على graphics buffer
│   ├── update.js           → دوال مساعدة (setText, setSceneLength)
│   ├── recording.js        → placeholder (التسجيل محذوف)
│   │
│   ├── 0_arcer.js          → نمط: Arcer
│   ├── 0_bend.js           → نمط: Bend
│   ├── 0_blank.js          → نمط: Blank (ثابت)
│   ├── 0_box.js            → نمط: Box
│   ├── 0_bugEyes.js        → نمط: BugEyes
│   ├── 0_bugEyesEE.js      → نمط: BugEyes (نسخة سريعة)
│   ├── 0_cloud.js          → نمط: Cloud
│   ├── 0_grid.js           → نمط: Grid
│   ├── 0_halo.js           → نمط: Halo
│   ├── 0_riseSun.js        → نمط: RiseSun
│   ├── 0_shutters.js       → نمط: Shutters
│   ├── 0_shutters2.js      → نمط: Shutters 2
│   ├── 0_shuttersEE.js     → نمط: Shutters (نسخة سريعة)
│   ├── 0_slotMachine.js    → نمط: SlotMachine
│   ├── 0_snap.js           → نمط: Snap
│   ├── 0_split.js          → نمط: Split
│   ├── 0_starburst.js      → نمط: Starburst
│   └── 0_twist.js          → نمط: Twist
│
└── resources/              → ملفات الخطوط (.otf / .ttf)
```

---

## 🎨 إضافة نمط حركة جديد

كل نمط هو **class** بنفس البنية:

```javascript
class MyAnimation {
  constructor(ramp_, inp_) {
    this.inp = inp_;          // النص
    this.ticker = 0;
    this.ramp = ramp_;        // 0 أو 1 (يتبادلان)
    // ... إعداداتك
  }

  update() {
    this.ticker++;
    // ... منطق الحركة
  }

  display() {
    background(bkgdColor);
    // ... رسم الإطار
  }

  removeGraphics() {
    // حرر أي createGraphics() هنا
  }
}
```

بعد إنشاء الملف، أضفه في `index.html` وأضف سطراً في `pickScene()` داخل `sketch_flash.js`.

---

## 🔤 إضافة خط جديد

1. ضع ملف الخط في مجلد `resources/`
2. في `sketch_flash.js` داخل `preload()`:
   ```javascript
   tFont[7] = loadFont("resources/اسم-الخط.ttf");
   ```
3. في `config.js` غيّر `fontIndex` إلى `7`

---

## 📦 المتطلبات

- متصفح حديث (Chrome / Firefox / Edge)
- Python (لتشغيل السيرفر المحلي)
- لا يوجد npm أو build tools

---

## 📄 الرخصة

يمكنك استخدام هذا المشروع وتعديله وتوزيعه بحرية.
